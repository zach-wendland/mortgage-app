# Frontend Environment Variables
# Copy this file to .env and configure as needed

# API Proxy URL - URL of the backend proxy server that securely handles API keys
# Defaults to http://localhost:3001 for local development
# In production, set this to your deployed proxy server URL
VITE_API_PROXY_URL=http://localhost:3001

# Note: API keys are now configured in server/.env (NOT exposed to frontend)
# See server/.env.example for server configuration
